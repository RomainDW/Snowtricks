<header class="masthead">
    <div class="container h-100">
        <div class="row h-100 align-items-center">
            <div class="col-12 text-center content-title">
                <h1 class="m-0 masthead__title">{{ trick.title }}</h1>
                <h2 class="m-0 masthead__subtitle">{{ trick.category.name }}</h2>
            </div>
        </div>
    </div>
</header>